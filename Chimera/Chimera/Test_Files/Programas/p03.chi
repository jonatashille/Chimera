sub main()
	var int a,b;
	scan(a);
	scan(b);
	if a>b then
		println("Primeiro valor Maior");
	else if a<b then
		println("Segundo valor Maior");
	else
		println("Valores iguais");
	end_if
	end_if

	/*while (a<20) do
		a = a + 1;
		println(a);
	loop*/

	/*repeat
		a = a + 1;
		println(a); 
	until
		(a<20);*/

	for a = 1 to 20 do
		println(a); 
	next

	println(b);
end_sub
